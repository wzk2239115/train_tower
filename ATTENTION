# convert 时 LIMIT 用法

# ❌ 这样 LIMIT 不会生效（python 直接调用不认 LIMIT 环境变量）
LIMIT=5000 python -m tower.cli convert --dataset blip3o_short

# ✅ 正确写法 1：用 --limit 参数
python -m tower.cli convert --dataset blip3o_short --limit 5000

# ✅ 正确写法 2：用脚本（脚本会读 LIMIT 环境变量）
LIMIT=5000 ./scripts/convert.sh blip3o_short

# blip3o 全量 convert（默认 fast：并行 tar 解压 + 扫盘写 jsonl，不做 PIL 重编码）
WORKERS=16 ./scripts/convert.sh blip3o_long
# 分两步（可断点续跑；已解压的 tar 子目录会 skip）
EXTRACT_ONLY=1 WORKERS=16 ./scripts/convert.sh blip3o_long
JSONL_ONLY=1  WORKERS=16 ./scripts/convert.sh blip3o_long

# 训练依赖（NEO + SenseNova-U1）

源码已 vendoring 在 `third_party/`，拷到内网服务器后无需再 clone / 建 symlink：

```bash
pip install -e ".[train]"
python -c "from tower.paths import ensure_train_paths; ensure_train_paths(); import neo.data; print('neo OK')"
MAX_STEPS=10 DATASETS=blip3o_short_pt ./scripts/train_smoke.sh
```

若 `third_party/NEO` 缺失，运行 `./scripts/vendor_third_party.sh` 重新同步。

# manifest 与 jsonl 不同步

训练读的是 `data/processed/manifest.json`，不是直接扫 jsonl。
convert 被 Ctrl+C 中断时，可能 **jsonl 已写好、manifest 未更新**，会报：

`ValueError: Dataset blip3o_short_pt not found in data_dict.`

修复（不重转数据）：

```bash
python -m tower.cli convert --refresh-manifest
python -c "import json; print(list(json.load(open('data/processed/manifest.json'))))"
MAX_STEPS=10 DATASETS=blip3o_short_pt ./scripts/train_smoke.sh
```

# smoke 训练与多卡

`train_smoke.sh` 用普通 `python` 启动（不是 torchrun）。在 H800 多卡机器上必须限制单卡，否则 HF Trainer 会套 `DataParallel` 导致 `StopIteration`：

```bash
# 默认已设 CUDA_VISIBLE_DEVICES=0；手动指定也行
CUDA_VISIBLE_DEVICES=0 MAX_STEPS=10 DATASETS=blip3o_short_pt ./scripts/train_smoke.sh
```

正式多卡训练用 `scripts/train_uw.sh` 等（内部 `torchrun`）。
